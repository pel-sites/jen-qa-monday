server {
    listen 8080;
    server_name localhost;
    root /usr/share/nginx/html;
    index production_board_structure.html qa_workflow_analysis.html index.html;

    location / {
        try_files $uri $uri/ /qa_workflow_analysis.html;
    }

    # Cache static assets
    location ~* \.(html|css|js|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1h;
        add_header Cache-Control "public, immutable";
    }
}
